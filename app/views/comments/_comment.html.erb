<tr>
    <td><%= comment.commenter %></td>
    <td><%= comment.body %></td>
    <td><%= link_to "Delete", [comment.post, comment ], data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</tr>